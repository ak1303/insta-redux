export const followersSelector = (state) => state.connections.followers;
export const suggestionsSelector = (state) => state.connections.suggestions;
export const pendingConnectionSelector = (state) => state.connections.pendingConnections
export const followingSelector = (state) => state.connections.following;